apiVersion: v1
kind: Service
metadata:
  name: {{ include "test-web-app.fullname" . }}
  labels:
    {{- include "test-web-app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "test-web-app.selectorLabels" . | nindent 4 }}
